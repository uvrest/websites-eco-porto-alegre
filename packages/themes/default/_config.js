// packages/themes/default/_config.js
export const themeConfig = {
    id: 'default',
    label: 'Uverest Default',
    supportedModes: ['light', 'dark'], // ['light'] para light-only, ['dark'] para dark-only
    defaultMode: 'light',
    allowUserToggle: true, // false para esconder/bloquear o toggle
};